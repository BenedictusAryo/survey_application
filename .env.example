# Django Settings
SECRET_KEY=your-secret-key-here-generate-new-for-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# For development (SQLite) - leave DATABASE_ENGINE commented or set to sqlite3
# DATABASE_ENGINE=django.db.backends.sqlite3

# For production (MySQL on cPanel) - uncomment and configure these:
# DATABASE_ENGINE=django.db.backends.mysql
# DATABASE_NAME=your_cpanel_database_name
# DATABASE_USER=your_cpanel_database_user
# DATABASE_PASSWORD=your_cpanel_database_password
# DATABASE_HOST=localhost
# DATABASE_PORT=3306

# Email Configuration (optional)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=your_smtp_host
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@domain.com
# EMAIL_HOST_PASSWORD=your_email_password

# CAPTCHA Settings (if needed)
# RECAPTCHA_PUBLIC_KEY=your_recaptcha_public_key
# RECAPTCHA_PRIVATE_KEY=your_recaptcha_private_key
